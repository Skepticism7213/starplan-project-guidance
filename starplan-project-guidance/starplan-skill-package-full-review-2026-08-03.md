# StarPlan Skill 包全面审查报告

日期：2026-08-03；前半部分状态更新：2026-08-04；二次更新：2026-08-04（P2 收尾修复与 W-1/W-7 合并入 main，见第十四节）
审查对象：`StarPlan/`（四核心 Skills 包）及其规划文档（`starplan-project-guidance/`）
当前代码基线：`e3dc79e`（`HEAD` 与 `origin/main` 一致；工作树干净，仅测试时间戳 artifact `confidence_test_results.json` 有变动）
官方赛题依据：
- 阿里云官网：<https://university.aliyun.com/action/tzbjbgs2026>（题目编号 XH-202619，赛道三方向三）
- 国家天文科学数据中心赛题解析：<https://nadc.china-vo.org/article/20260624094452>

> **阅读说明**：第 1--8 节已经按当前项目进度重写，是先读的有效结论；第 9--13 节保留真实 Qwen、P2、Batch D/E 和本机复现的详细证据；第十四节为 2026-08-04 二次更新（P2 收尾修复、W-1/W-7 合并结果与新发现的边界）。早期 `d9b1f22`、“185 passed / P1 未完成 / Qwen 证据为零”等内容只在后文作为历史实施记录，不再作为当前状态判断。

> **当前验收口径**：离线回归固定使用项目 `.venv`、`STARPLAN_MODEL_MODE=offline` 和系统 Temp 隔离目录；真实 Qwen 证据单独记录，不与离线通过混写。当前可承诺的是“P0 可信输出 + P1 竞赛闭环已通过，2026-08-03 审查的代码级发现已全部关闭（离线回归 228 条），P3/P4 交付证据仍在补齐”。

---

## 一、执行摘要

### 1.1 总体结论

StarPlan Loop 当前已经从“可信输出架构开发”进入“竞赛交付准备”阶段。`6d2bbbb` 基线上的 P0（Claim/fail-closed/确定性渲染）和 P1 Batch D/E（现实活动时段、三类分众输出、复盘到下一轮输入、二次运行 before/after）均已通过本机离线验收；兼容端点下的真实 Qwen canary、结构化入口和 Chat 工具链也已跑通。

当前还不能宣称最终交付完成，剩余工作集中在证据和提交层，而不是继续扩展核心功能：

1. **智能体加载证据尚未固化**：已有兼容端点 E2E，但还需要把 Skills 在目标 Qwen/百炼智能体环境中的加载、触发、返回过程整理成可复现记录；
2. **P3 复现包尚未入交付目录**：三类案例的输入、中间结果、最终输出、人工确认和第二环境复跑记录尚未形成固定脱敏包；
3. **P4/P5 材料尚未冻结**：技术方案 PDF、演示视频、源码/依赖/许可证清单和最终敏感信息检查仍需完成。

2026-08-04 当日完成了最后一批代码级收尾：R-01/W-04/R-06/I-3 卫生修复、W-1 替代目标真实计算与验证、W-7 公共返回合同文档化，已合并至 main（`e3dc79e`）并推送；离线回归从 211 条增至 228 条全绿。至此本审查第 5.3/5.4 节中与代码直接相关的发现全部关闭，剩余工作均为证据与材料层，不再涉及核心代码改动（除非 P3/P4 过程暴露阻断性缺陷）。

### 1.2 获奖潜力判断

项目目前的竞争力不再依赖“还要再做一个功能”，而在于能否把已经完成的技术能力压缩成评委可检查的证据链。最值得呈现的三点是：

- "Qwen 不产生科学数值 + 程序渲染事实句 + 全链路证据"这一设计本身稀缺且可被评审快速验证；
- 复盘结果能够生成下一轮合法输入并重新进入 runner，能直接证明“围绕明确任务形成闭环”；
- 同一 Claims 可生成组织者、讲解员和学习者视图，且不可观测时 fail-closed 并给出替代活动。

初赛是材料打分，未落盘的调用凭证、运行记录和人工校验不会自动计分。因此当前最重要的竞争动作是冻结一条 10 分钟内可复现的演示主线，而不是继续增加行星、天气或复杂前端等非 MVP 功能。

---

## 二、审查方法与实际执行证据

| 检查项 | 结果 |
|---|---|
| 远端基线核对 | 代理 `git fetch origin` 成功；二次更新时 `HEAD = origin/main = e3dc79e`，`origin/main...HEAD = 0 0` |
| Python 编译 | `python -m compileall -q starplan_skills scripts tests`：PASS |
| 固定示例 | `scripts/validate_examples.py`：3 passed, 0 failed |
| Layer 2/3 科学与 Claim 校验 | 10 轮、0 unique issues |
| 科学交叉校验 | `scripts/cross_validate.py`：12/12 PASS |
| 离线全量回归 | `scripts/run_offline_ci.bat`：**228 passed, 0 failed**（`e3dc79e` 合并后门禁，约 111s；此前基线 211 passed / 68.01s）；编译、示例和网络 tripwire 同时通过 |
| README/闭环案例 | M31 可观测、M42 不可观测、案例三复盘和 `run_loop` 均可离线运行；最近一次约 3–4s/案例，二次闭环约 4s |
| Windows 环境/编码 | `.venv` 初始化脚本可复用；默认 GBK Python 导入包后标准流为 UTF-8；中文输入输出通过 |
| 真实 Qwen | 兼容端点下 `qwen3.7-plus`、`qwen3.7-max`、`qwen3.8-max` canary 均成功；项目在线套件在显式可写 basetemp 下 **9 passed，172.92s** |
| Git 工作树 | 此前未提交的环境、README 和报告归档变更已在 `3c902e6` 入库；二次更新时工作树干净（仅 `confidence_test_results.json` 时间戳 artifact 变动，按团队纪律不提交） |

> 说明：离线回归与在线证据分开统计。带 `.env` 的机器若直接执行未隔离的在线测试，结果会受 Key、模型权限、代理和临时目录 ACL 影响；正式复现应使用 `run_offline_ci.bat`，在线证据则单独保存脱敏日志和截图。

---

## 三、现状盘点：项目自评的成功标准逐条对照

以下对照项目计划第 13 节"项目成功标准"：

| 成功标准 | 当前状态 | 证据/缺口 |
|---|---|---|
| 可直接调用的 Skills 包 | ✅ 已达成 | `starplan.run` / `run_starplan()`，4 个核心 Skills + 统一 Schema；README 有离线入口 |
| 至少一种 Qwen 智能体环境完成加载→触发→返回 | ⚠️ 技术链路已通，加载证据待固化 | 兼容端点真实 E2E 与 Chat 工具链通过；尚未形成目标智能体平台的安装/加载截图和固定步骤 |
| 至少一个核心案例完整闭环 | ✅ 代码闭环已达成 | `review_observation` 生成合法 `next_activity_input.json`，`run_loop.py` 二次调用 runner 并生成带 `cause_id` 的 before/after |
| 至少三类任务可复现运行记录 | ⚠️ 代码可复现，交付证据未固化 | M31、M42、案例三均可离线运行；`runs/*/` 被 gitignore，需另存脱敏提交包 |
| 同一 Claims 生成组织者/讲解员/学生三类输出 | ✅ 已达成 | Batch D 三视图分别渲染，纳入交付合同和 trace 校验 |
| 科学可见窗口与现实活动时段分离 | ✅ 已达成 | `activity_slot_policy_v1`；M31 现实活动约 90 分钟，科学窗口单独保留；M42 不生成虚假 slot |
| Review 下一轮输入可再进 runner 并产生可追差异 | ✅ 已达成 | 白名单补丁写入 `next_activity_input.json`，第二次运行成功，before/after 每项引用 `cause_id` |
| 所有用户可见事实可经 Claim 映射溯源 | ✅ 核心输出已达成 | 活动包、三视图、Chat 公共返回走 Claim/RenderedDocument 门禁；P3 仍需随案例包保存证据文件 |
| 无来源事实率 0、阻断原文泄漏率 0、映射覆盖率 100% | ✅ 离线测试达成 | 对抗测试、交付合同和 228 条离线回归覆盖 |
| 六类终态均有确定性输出 | ✅ 基本达成 | 可观测、不可观测、数据不足、工具错误、验证阻断均有 fail-closed 路径；目标歧义仍需人工确认入口演练 |
| Manifest / Validation Report / 审计 / 公共返回状态一致 | ✅ 已达成（以测试为准） | RunOutcome、manifest、validation 和公共输出同源；P3 需保存样例供评委检查 |
| Qwen 作用、边界与调用证据清晰 | ⚠️ 代码已清晰，材料待整理 | 模型只选择受信 Claim/表达计划；真实调用已成功，仍需脱敏日志、截图和模型配置说明 |
| 评委仅读 20 页材料和 10 分钟视频即理解价值 | ❌ 未达成 | PDF、视频和固定演示脚本尚未冻结 |
| 团队成员能解释每个模块输入/输出/依赖/失败处理 | ⚠️ 待演练 | 计划和 README 已有内容，需要一次按评委视角的全流程彩排 |

---

## 四、赛题要求逐条对照（方向三）

官方方向三原文（节选）：

> "鼓励团队开发一个面向具体任务环节的天文 skills 包……把原本分散、老旧或只适合人工操作的天文能力，改造成**可运行、可检查、可复现、可复用**的 AI Ready 组件……技能包应说明**服务对象、任务边界、输入输出、触发条件、依赖来源和失败处理方式**，并**保留关键中间结果**，方便评审复现和判断输出可靠性。"
>
> "初赛评价将关注技能包是否围绕明确任务形成闭环，而不是简单堆叠功能。如能对老旧但常用的天文工具进行接口标准化、运行加速或可复现重构，可考虑加分。"

| 官方要求 | 当前状态 | 证据 | 差距评价 |
|---|---|---|---|
| 可运行 | ✅ | 三案例、二次闭环和 `run_offline_ci.bat` 均可运行；211 条离线测试通过 | 达标 |
| 可检查 | ✅ | Claim Registry、render_trace、run_outcome、validation_report、交付合同门禁 | 达标且是主要优势 |
| 可复现 | ⚠️ | 固定输入、离线运行策略和环境脚本已具备；第二环境和固定案例包尚未交付 | 需要 P3 证据闭合 |
| 可复用 | ⚠️ | 4 个 Skill 有独立 Schema、触发和失败说明；目标智能体加载步骤/截图尚未固化 | 需要 P2 交付证据 |
| 服务对象 | ✅ | 中小学生活动、高校社团、科普组织者和非专业带队者 | 达标 |
| 任务边界 | ✅ | 单晚深空/亮星观测；不含行星、天气、望远镜控制等后置范围 | 边界清楚，避免过度承诺 |
| 输入输出/触发条件 | ✅ | `schemas.py`、`skills.yaml`、README 和四个 Skill 入口 | 达标 |
| 依赖来源 | ✅ | requirements、README 许可证表、catalog/SIMBAD provenance | 达标 |
| 失败处理 | ✅ | 可观测/不可观测、数据不足、工具错误、验证阻断和离线回退均确定性处理 | 达标且超出平均 |
| 保留关键中间结果 | ⚠️ | 运行目录包含 manifest、claims、trace、outcome 等；`runs/*/` 被忽略，人工确认仍需签名样例 | P3 必须固化 |
| 围绕明确任务形成闭环 | ✅ | 计划→计算→活动包→观测日志→复盘→下一轮输入→二次运行→before/after | P1 Batch D/E 已关闭 |
| 基座模型使用 Qwen/百炼并给调用凭证或截图 | ⚠️ | 兼容端点三个模型 canary、结构化和 Chat E2E 已通过；正式材料还缺脱敏凭证/加载截图 | P2 证据收尾 |
| 技术方案文档 PDF ≤ 20 页 | ❌ | 尚未冻结 PDF 内容和页数 | P4 待完成 |
| 附加：可交互前端 / 10 分钟内演示视频 | ❌ | 尚未制作；视频应优先于独立前端 | P4 待完成。团队曾提出四页前端方案，2026-08-04 评审结论：非评分项、与冻结计划（agents.md post-MVP 条款）冲突且稀释"面向 AI"叙事，建议缓做或收窄为证据链可视化视频素材；若立项须先修订项目计划 |
| 加分：对旧工具接口标准化/加速/复现重构 | ✅ 有可展示素材 | 已将 Astropy/astroplan 封装为离线可审计 Skill，并提供一键回归；尚缺传统人工流程对照数据 | 材料中整理为加分证据 |

**时间口径：**

| 事项 | 项目计划/转移日志 | 官方（阿里云官网 + NADC） | 处理建议 |
|---|---|---|---|
| 作品提交截止 | 负责人确认和项目计划 | **2026-09-01 00:00（北京时间）** | 按 09-01 作为不可延期的内部冻结线；旧报告引用的 09-05 网页日期未重新核验，不作为当前倒排依据 |
| 材料形式 | 负责人提交清单 | 一份不超过 20 页的 PPT/PDF，附 Skills 包、源码、报告和运行记录 | 以可直接加载/复现的 PDF 技术方案为主，视频作为附加交付 |
| 报名截止 | 未提及 | 2026-06-30 已截止（报名系统） | 需确认团队是否已完成官网报名（逾期不可补） |
| 初赛 | 材料打分制，无答辩 | 以提交材料和运行证据为准 | 所有关键判断必须在 PDF/运行包中自证，不能依赖现场解释 |
| 决赛 | 未提及 | 2026-11 擂台赛 | 材料/视频需支撑决赛答辩 |

---

## 五、深度审查发现

### 5.1 值得肯定的部分（当前优势）

1. **确定性计算与模型边界执行得彻底**：`observability_plan.py` 全部数值来自 Astropy/astroplan；Qwen 只产出 `ExpressionPlan`（claim_id + variant_id），最终事实句由程序渲染；12/12 交叉校验通过，我抽查的 M31 数值与物理预期一致。
2. **幻觉防护是三层结构且经过对抗测试**：Claim 准入（8 步验证）→ 确定性渲染（RenderedDocument 双向覆盖）→ 交付合同门禁（7 步 post-render 检查）；`test_mock_qwen_adversarial.py` 覆盖伪造 claim、伪造 variant、prohibited、注入、空响应、冲突等攻击面。
3. **离线策略进入产品入口**：`astro_runtime.configure_astronomy_runtime()` 在 runner 与直接调用 compute_observability 之前生效，README 三案例在无 Key、无缓存、代理不可达条件下正常终态。
4. **可审计状态机**：RunOutcome 三轴状态（业务/验证/交付）正交，manifest、validation report、公共返回同源。
5. **数据治理认真**：150 目标目录带 provenance、SIMBAD 交叉校验、10 轮 Layer 2/3 校验；隐私模块定义了审计文件与导出脱敏。
6. **文档纪律好**：多轮 error-check/phase-plan 报告齐全，README 对未完成项有诚实标注。

### 5.2 当前未闭合的交付缺口（按优先级）

#### R-P2 智能体加载与调用证据仍需固化

兼容端点下的 `qwen3.7-plus`、`qwen3.7-max`、`qwen3.8-max` canary、结构化入口和 Chat 工具链均已真实跑通；但当前证据主要是本机运行日志，还没有一份面向评委的“安装/加载 Skills → 自然语言触发 → 工具调用 → Claim 渲染返回”固定记录。P2 的代码问题已关闭，剩下的是目标智能体平台、模型配置、调用凭证截图和脱敏日志的交付整理。

#### R-P3 三案例复现包与人工确认未固化

M31 正常可观测、M42 不可观测、案例三复盘闭环都可以离线重跑，但 `runs/*/` 默认被忽略。需要另存固定输入、关键中间结果、最终输出、`run_outcome`、manifest、validation、Claim trace、人工确认入口和 SHA-256 清单，并在第二台 Windows 或干净环境复跑一次。中小学活动还需要保留安全项的人工确认记录；实地观测若未完成，必须明确标成桌面演练或模拟数据。

#### R-P4/P5 提交材料和演示主线未冻结

20 页内技术方案 PDF、10 分钟内视频、源码/依赖/许可证清单、脱敏运行包和最终敏感信息扫描仍未形成提交版本。初赛为材料打分，不能把“代码已经通过”当成“评委已经看到”。应先冻结一条结构化入口主线，再决定是否把 Chat 作为补充，不再在材料冻结前插入非 MVP 功能。

#### 当前 WARNING

- ~~`tests/test_qwen_integration.py` 的 skip 条件依赖 Key，带 `.env` 全量 pytest 可能误触网络~~：**已关闭（R-01，2026-08-04）**——offline 模式现无条件跳过在线测试，见第十四节。
- `requirements.txt` 仍是下限约束，尚无跨机 lock（R-02）；第二环境记录必须同时保存 Python、依赖、操作系统、时区和命令，随 P3 交付包一起完成。
- 旧 `.ci_tmp` 测试缓存存在 ACL 残留（2026-08-04 二次更新时 `git fetch` 仍报该目录 Permission denied），新 CI 已使用系统 Temp 唯一路径，不影响测试；有权限时再定点清理。
- ~~替代目标未经验证~~：**已关闭（W-1，2026-08-04）**——替代目标改为两阶段真实计算验证，见第十四节。人工确认和独立复现证据仍应在材料中标注"程序结论 / 待人工确认 / 模拟演练"的边界，避免把演示数据写成实地事实。

### 5.2A 早期 CRITICAL 记录（历史快照，不作为当前结论）

以下 C-1 至 C-5 是 `d9b1f22` 审查时的原始发现。Batch D/E、兼容端点适配和当前离线回归已改变其中多数结论；保留它们是为了追踪修复来源，不能用来判断当前 `6d2bbbb` 状态。

#### C-1 闭环未闭合：复盘结果无法重新进入 runner（最大卖点缺失）

- 位置：`observation_review.py` 的 `_build_revised_plan()` / `revised_plan.json`；`runner.py` 无 next-input 处理。
- 事实：`revised_plan.json` 是自定义结构（`revisions[]`、`suggestions[]`），不是 `StarPlanInput`；项目计划要求的 `next_activity_input.json`、before/after 活动包对比均不存在。
- 影响：方向三评分明确"关注技能包是否围绕明确任务形成闭环"；当前演示只能展示"计划→复盘"，不能展示"复盘→下一轮计划真实变化→重新渲染活动包"，等于把最核心的差异化证明缺失。
- 证据：`review_report.md` / `revised_plan.json`（本次实跑案例 3）与 README"尚未完全闭合的项目"自认一致。

#### C-2 现实活动时段与三类分众输出未实现，活动流程"通宵式"不可用

- 事实：M31 案例（2026-10-17，济南四门塔）活动流程为 18:58 准备 → 19:13 开始 → "19:13–04:28 观测进行中" → 04:54 结束，共约 9 小时；无 60–120 分钟现实活动 slot、无准备/收尾独立安排。
- 事实：`outreach_pack` 只有单一受众表达（按字符串含"新"选 beginner 变体），组织者/讲解员/学生三类视图、未成年人安全确认模板（P1 Batch D）均未实现。
- 影响：直接扣"内容转化清晰度"与"真实场景使用价值"；评审一眼就能看出产物不适合真实校园活动。

#### C-3 真实 Qwen 调用证据为零（官方硬性要求 + 技术深度失分）

- 事实：`tests/test_qwen_integration.py` 共 9 个在线测试全部 skip；仓库无 `.env.example`（README 却让用户 `cp .env.example .env`）；无百炼调用截图/凭证；`qwen_client.py` 默认模型名 `qwen3.7-max` 等未经过任何在线验证；`call_qwen*` 无超时参数。
- 影响：官方"技术基础"要求提供调用凭证或截图；技术深度评分中"模型、智能体、技能设计完整性与结果校验"无法被证明。若提交时仍如此，可能被视为未实际使用阿里云能力。

#### C-4 三组完整运行记录未入库，评审无法直接复现

- 事实：`StarPlan/.gitignore` 忽略 `runs/*/`，仓库内 0 个运行产物；`数据验证日志/` 是过程日志而非"输入+中间结果+最终输出+人工校验"的完整案例包；人工核对项只是清单，无签名/确认记录。
- 影响：官方要求"保留关键中间结果，方便评审复现和判断输出可靠性"；评审需要自己安装运行，或团队在提交包内另附 runs。

#### C-5 提交材料全部未做，且截止日期文档错误

- 事实：无 20 页内 PDF 技术方案、无演示视频、无复现包清单；项目计划把截止写成 09-01，官方为 09-05（若团队按 09-01 倒排会白白损失 4 天，但更危险的是按 09-01 提交前仍做不完）。
- 影响：应用潜力 30% 中的"演示、交互入口与交付完整度"基本只能拿低分。

### 5.3 早期 WARNING 记录（历史快照；当前状态以 5.2 和第十三节为准）

> **2026-08-04 状态标注**：已关闭——W-1（替代目标两阶段真实计算验证，`0edf65b`）、W-3（v0.6.0 兼容端点超时/重试）、W-6（v0.6.0 `.env.example` 与版本统一）、W-7（按负责人决策文档化而非置空，`9922606`）。仍开放——W-2（Review 报告未纳入渲染门禁，维持"分析性输出"定位）、W-4（推荐窗口策略单一，负责人决定保留现值）、W-5（覆盖范围窄，MVP 边界，技术方案中须写明）、W-8 的第二环境部分（canary 已完成，干净环境复跑归 P3）、W-9（结构化 NEEDS_CONFIRMATION 未实现）、W-10（设备适配假设须在技术方案中写明）。

- **W-1 替代目标建议未经验证**：`_generate_alternatives()` 按月份静态表给 M13/M57 等，未对建议目标在"该地点该日期"实际计算可观测性；`blocking.alternatives` 却以 DERIVED_FACT 输出"当季更适合观测的目标"。建议对每个替代目标真实跑一次可观测性，或降级为"候选目标，需人工复核"。
- **W-2 Review 输出未纳入 Claim/交付门禁**：`review_report.md` 由字符串拼接生成，无 render_trace/双向覆盖/泄漏检查；改进建议是自由文本（虽为规则库产生）。P1 的 Review ID-only 协议未实现。
- **W-3 在线调用无超时/重试/熔断**：Qwen 调用异常会回退模板，但没有 30–35 秒超时控制，演示时网络挂起可能卡死入口；Chat 模式同理。
- **W-4 推荐窗口策略单一**：取"最长可见窗口"而非综合最优（如避开高月光、低空、暮光加权），也无反事实解释（提前/延后 1 小时变化）。可作为 P1 增强。
- **W-5 覆盖范围窄**：150 目标 + 8 城市；`date_range` 多天只算第一晚；无行星、无天象、无地平线遮挡。MVP 可接受，但需在技术方案中明确边界与扩展路径，避免评审按"天文助手"期望扣分。
- **W-6 文档与实现漂移**：README 写 "184 passed"，实跑 185；README 提 `.env.example` 但文件缺失；README 协作规范仍是五人分工（与计划三角色不符）；`run_outcome.json` 的 `state_transitions` 未含最终 RENDERED（写入顺序小瑕疵）；`StarPlan/docs/` 在 README 结构树中列出但不存在；根 README 仍写"仓库仅含规划材料"（已过时）。
- **W-7 BLOCKED 时结构化公共返回仍含 target/plan 数值**：测试合同只保证 `outreach_pack=None`；若评审按"公共返回 0 条事实"字面理解，会质疑合同不一致。建议文档明确约定，或按更严格合同置空。
- **W-8 独立环境复现与真实百炼 canary 仍为遗留项**（前轮报告 W-R2/W-R3 未关闭）。
- **W-9 人工确认机制未真正落地**：目标歧义以抛异常表达而非结构化 `NEEDS_CONFIRMATION` 返回；活动包人工核对无"确认前/后差异"记录。
- **W-10 派生可见性规则偏乐观**：`equipment.match`、肉眼/双筒可见性用简单星等阈值（已在 claim 内加 caveat），但"当前设备适合观测此目标"对初学者仍可能过度承诺，技术方案中应写明假设与限制。

### 5.4 早期 INFO 记录（历史快照；已处理项不再重复安排）

- Windows 默认代码页下 CLI 中文输出乱码（不影响文件，但影响演示观感），建议 `run_case.py` 输出前设置 UTF-8 或提示 `chcp 65001`。
- 中文受众看到 `Andromeda` 星座英文名与 `RA/Dec` 英文标签，建议增加中文变体。
- ~~`MoonInfo.moonrise/moonset` 恒为 None~~：**已关闭（I-3，2026-08-04）**——按 5 分钟网格+二分法真实计算，事件不在窗口内时返回 None 属合法语义（有回归测试）；`observation_log.json`、`review_trace.json` 未列入 README 产物表仍为遗留小项。
- 审计时间戳硬编码 UTC+8，若用户使用其它时区地点，时间戳仍为北京时间（可接受但需注明）。
- 目标目录中 M31 Dec 41.2688° 与项目文档示例 41.2692° 有 0.0004° 差异，属正常精度差异，建议统一表述。

---

## 六、当前收尾路线图：从已完成代码到可评分提交

原则：**先固定证据，再冻结材料，最后只做回归修复**。P0 和 P1 已验收，不再反复重写 `claims.py`、`rendering.py`、`runner.py` 等受保护架构，也不插入行星、天气、复杂前端等非 MVP 功能。

### P2 收尾：智能体加载与在线证据（2026-08-04 至 2026-08-07）

- 选择一个团队实际可用的 Qwen/百炼模型和一个目标智能体加载形态，固定 `base_url`、模型名、超时、重试和离线回退说明。
- 从干净目录按 README 加载 Skills，录制一次“自然语言触发 → 四 Skill/工具链 → Claim 渲染 → 公共返回”的完整过程；保存脱敏截图、`model_call_log.jsonl`、`run_outcome.json` 和版本信息。
- 失败条件只允许回退到确定性模板，不把 Qwen 原文当最终输出；不再为了追求更短延迟无边界切换模型。
- **验收标准**：目标智能体至少完成一次真实加载和一次成功调用；同一输入在 `STARPLAN_MODEL_MODE=offline` 下仍能在 30 秒内确定性完成；Key、代理和个人路径不进入材料。

### P3 交付证据包（2026-08-05 至 2026-08-15）

- 固定 M31 正常可观测、M42 不可观测/替代方案、案例三复盘闭环三份脱敏运行包，包含输入、关键中间结果、最终输出、人工确认入口、manifest、validation、trace、outcome 和 SHA-256 清单。
- 在第二台 Windows 或干净 CI 复跑离线命令，记录 Python/依赖/OS/时区、命令和耗时；若实地观测尚未完成，明确标注桌面演练或模拟数据。
- 请一名物理教师、天文社成员或有经验爱好者完成最小科学复核，并保留确认项与未确认项。
- **验收标准**：第二环境离线零失败；三案例终态、关键 Claim 和 before/after 与主环境一致；人工确认状态可追溯。

### P4 材料与演示冻结（2026-08-10 至 2026-08-25）

- 编写不超过 20 页的 PDF 技术方案：问题、四 Skill 清单、流程图、上下文/Claim 防幻觉架构、三案例记录、数据来源与许可证、反馈闭环、安装和调用示例。
- 制作不超过 10 分钟的视频主线：M31 计划与三视图 → M42 fail-closed/替代活动 → 案例三复盘和 before/after；Chat 只在稳定时作为补充。
- 把源码、依赖说明、许可证、复现脚本、脱敏运行包和隐私扫描结果组成单一提交清单。
- **验收标准**：PDF 页数、视频时长、链接和目录逐项核对；不打开 `.env`，不出现 API Key、代理端口或本机绝对路径。

### P5 冻结与提交（2026-08-26 至 2026-09-01 00:00）

- 停止功能开发，只允许修复阻断性错误；从干净环境重装并运行离线 CI、三案例和材料敏感信息扫描。
- 由另一名成员检查完整 diff、运行证据和提交包清单；以负责人确认的 2026-09-01 00:00（北京时间）作为不可延期内部硬截止。
- **验收标准**：评委不依赖团队口头解释即可安装/加载、理解流程、看到三类任务证据并复现离线结果。

### 6A 早期路线图（历史快照，仅供追溯）

### P1 Batch D（8/3–8/8，本周）：现实活动时段 + 三类分众输出

- 新增 `activity_slot_policy_v1`：从科学窗口确定性选出 60–120 分钟现实活动时段（含准备/收尾各 15–30 分钟、可选 preferred start、人工确认状态）；`recommended_window` 保持科学窗口语义。
- 新增 `AudienceProfile`（age_band、experience_level、requested_views），同一 Claims 渲染 organizer / facilitator / learner 三视图；科学句与数字映射同一 claim_id。
- 未成年人场景增加 `youth_activity_policy_v1` 安全项与"待人工确认"标记（不采集隐私）。
- **验收标准**：M31 同时展示科学窗口与 90 分钟活动 slot；M42 不生成虚假 slot；三视图无事实冲突；新增测试全绿；更新 README/skills.yaml 只声明已实现能力。

### P1 Batch E（8/5–8/13）：可执行下一轮输入 + before/after

- `review_observation` 接收原始 `StarPlanInput` 规范化副本；白名单修订字段（如 ActivityPreferences、设备准备步骤）；输出符合 `StarPlanInput` 的 `next_activity_input.json`；通过 CLI/测试二次调用 runner 重跑并渲染新活动包；生成 before/after 对比表，每项变化引用 cause_id。
- **验收标准**：`next_activity_input.json` 通过 Schema；二次运行成功且不重复触发 Review；至少一个时间/设备/流程字段可见变化；删除证据后对应 patch 消失或降级为待确认。

### P2（8/10–8/17）：真实 Qwen 接入与智能体加载证据

1. 先跑 `scripts/test_qwen_connection.py` 与在线 canary，确认 `qwen3.7-max` 等模型名真实可用（不可用则改配置并记录）。
2. 为 `call_qwen*` 增加 30–35 秒超时、1 次重试、失败安全回退。
3. 选定一种"直接加载到智能体"的形态（百炼应用 / OpenAPI / MCP / QoderWork），写薄适配层，不复制核心逻辑。
4. 录制一次真实自然语言触发→工具链→Claim 渲染→返回的全过程，保存脱敏调用凭证截图与 `model_call_log.jsonl`（含真实 `type=model_call`）。
- **验收标准**：至少一次真实 Qwen 调用成功并留下审计证据；无 Key/无网络时仍按离线模板交付；四 Skill 的触发/输入/输出/失败说明更新进 `skills.yaml`。

### P3（8/14–8/22）：三案例固化 + 独立复现 + 人工复核

- 固定 M31 正常、M42 不可观测+备选、复盘闭环三类完整运行记录（输入、中间结果、输出、人工确认签名）；把 runs 作为提交包内容（建议同时提交到仓库或 `submission/runs/`）。
- 第二台电脑/干净 CI 复跑三案例并记录差异；完成一次小规模实地观测或明确标注的桌面演练；请 1 名物理教师/天文社成员做外部科学复核。
- **验收标准**：新环境零失败、三案例 <30 秒终态；每份案例包有人工确认记录；无来源事实率 0、泄漏率 0、映射覆盖率 100% 的测试证据随包附上。

### P4（8/18–8/27）：提交材料

- 20 页内 PDF 技术方案（官方要求的章节：问题与方法、架构、上下文工程、测试案例、数据来源、反馈迭代、源码入口）。
- 6–8 分钟演示视频（主线：一次完整闭环 + 一次不可观测 + 一次复盘→新计划）。
- 对照实验证据：裸 Qwen 与 Qwen+StarPlan 对同一问题的回答对比（可在 P2 后补）。
- 提交包清单：源码、requirements、许可证、三案例运行记录、复现脚本、隐私检查结果。
- **验收标准**：PDF ≤ 20 页；视频 ≤ 10 分钟；提交清单逐项打勾；敏感信息扫描通过。

### P5（8/28–9/5）：冻结与提交

- 停止加功能；全量回归；第二环境最终复跑；按官网要求打包（命名：学校-姓名-作品名-联系电话），上传网盘并附链接、提取码、截图；确认报名表盖章与申报系统一致。

### 加分项（仅在 P1–P3 全部完成后选做）

1. ~~替代目标自动验证~~ ✅ 已完成（2026-08-04，`0edf65b`）：两阶段真实计算验证（粗筛+完整可观测性复核），见第十四节。
2. 反事实解释：提前/延后 1 小时、更换目标/日期时各项风险如何变化。
3. 传统人工流程对照实验（完成时间、漏检项、新手解释能力问卷）。
4. 一键复现命令/脚本（当前 `run_offline_ci.bat` 已接近，补一个三案例一键包）。
5. 输出 Stellarium/Aladin 复现链接或脚本（作为人工复核出口，不做自研星图）。

---

## 七、当前风险与控制

| 风险 | 等级 | 当前控制与剩余动作 |
|---|---|---|
| 目标智能体加载方式或模型权限变化 | 高 | 只冻结一个已验证模型；保存配置说明和脱敏凭证；离线模板作为演示兜底 |
| 三案例运行证据和第二环境尚未固化 | 高 | P3 固定输入/中间结果/输出/Hash，并安排一次干净环境复跑 |
| PDF、视频和提交清单延期 | 高 | P4 与 P3 并行，先冻结演示主线，09-01 00:00 前停止功能开发 |
| 真实活动或中小学安全确认无法完成 | 中 | 明确标注桌面演练/模拟数据；保留成人陪同、监护许可等待确认项，不虚构实地事实 |
| 带 `.env` 直接运行在线测试、Windows ACL 或编码差异 | 中 | 使用 `run_offline_ci.bat`、项目 `.venv`、系统 Temp 和 UTF-8 入口；在线测试单独执行 |
| 依赖版本漂移和外部 Qwen 服务不稳定 | 中 | 保存环境清单和模型耗时；材料中同时提供确定性离线复现路径 |

### 7A 早期风险清单（历史快照）

| 风险 | 等级 | 缓解 |
|---|---|---|
| 真实 Qwen 模型名/限额/网络环境未知 | 高 | 8/10 前完成 canary，失败立即换模型名/平台 |
| 闭环功能延期导致卖点缺失 | 高 | 8/13 前必须完成 Batch E；不再插队新功能 |
| 独立复现环境（第二台电脑/CI）Windows ACL 问题 | 中 | 提前安排，参考 W-R2 的临时目录隔离方案 |
| 评审质疑替代目标、推荐窗口等科学细节 | 中 | 技术方案中写明假设、阈值与校验方法 |
| 团队人手（2+1）不足 | 中 | 材料与代码并行：A 做材料、B 做 P1、C 做复现 |
| 未确认是否完成官网报名（6/30 已截止） | 高 | 立即确认报名状态，这是硬门槛 |

---

## 八、当前结论

StarPlan 的确定性天文计算、Claim/fail-closed 防护、现实活动时段、三类分众输出和复盘后二次运行已经在当前基线完成并通过本机离线验证；兼容端点下的真实 Qwen 调用也已证明模型可以被限制在受信 Claim 和表达计划内。2026-08-04 的收尾批次（`f8b6d26`/`0edf65b`/`9922606`，合并 `e3dc79e`）进一步关闭了本审查提出的全部代码级发现（R-01/W-04/R-06/I-3/W-1/W-7），离线回归达到 228 条全绿；此后的工作不再涉及核心代码，除非 P3/P4 过程中暴露阻断性缺陷。项目当前的主要问题已经从“核心功能缺失”转为“交付证据是否足够让评委检查”。

在 2026-09-01 00:00（北京时间）内部硬截止前，团队必须完成三件事：把 Skills 在目标智能体中的加载和调用证据固化，把三案例与第二环境复现整理成脱敏提交包，把技术方案 PDF 和视频冻结。只要这三项按验收标准完成，作品就能把“科学正确、低幻觉、可复现、能闭环”从代码承诺转成材料可见的竞争力；在此之前，不应把项目描述为最终完成。

### 8A 早期结论（历史快照）

以下段落保留早期审查时的判断，当前应以本节和第十三节为准。

StarPlan 的**架构和工程质量已经具备获奖所需的"科学正确性 + 幻觉防护 + 可复现"地基**，这是很多参赛队不具备的。早期主要矛盾是闭环未闭合、Qwen 未真实调用、案例与材料未交付；这些代码层问题已在后续批次处理，材料和证据层仍在收尾。

---

## 九、真实 Qwen 实测补充（2026-08-03，三把 Key 实测）

> 本节为拿到用户提供的 API Key 后的真实在线审查结果。**Key 仅用于本会话环境变量，未写入仓库任何文件；运行目录与仓库全文检索均未发现 Key 泄漏。** 由于三把 Key 均与当前 `qwen_client.py` 的调用方式不兼容，在线链路通过"临时兼容端点适配层"（仅存在于审查会话内存中，未入库）完成验证。

### 9.1 三把 Key 实测矩阵

| Key | 声称 | 原生 DashScope 端点（当前代码路径） | OpenAI 兼容端点 | 结论 |
|---|---|---|---|---|
| Key-1（qwen3.7） | qwen3.7 | 曾连通 qwen3.7-max（本日早些时候成功）；之后返回 401 Invalid API-key | 401（同） | 已失效/被轮换；失效后 8 个集成测试失败、1 个通过，属 Key 状态而非代码缺陷 |
| Key-2（qwen3.8） | qwen3.8 | qwen3.7-max / qwen3.8-max-preview / qwen3.8-max 全部 403 或 400 | **qwen3.8-max 正常**（单轮与 JSON 均成功） | Key 有效，仅授权 qwen3.8-max，且只走兼容端点 |
| Key-3（qwen3.7） | qwen3.7 | 全部 403/400 | **qwen3.7-plus 正常**（单轮、JSON、工具调用均成功） | Key 有效，仅授权 qwen3.7-plus，且只走兼容端点 |

**核心结论（C-3 升级）**：问题已从"没有真实调用证据"升级为"**即使有 Key，当前代码也无法调用**"。`qwen_client.py` 使用 DashScope 原生 `Generation.call` + 固定模型名（qwen3.7-max/qwen3.7-plus/qwen3.8-max-preview），而用户拿到的三把 Key 均只授权兼容端点下的特定模型。必须在 P2 中新增 **OpenAI 兼容端点适配层**（base_url 可配置、模型名可配置、超时与重试），否则"必须实际使用阿里云 Qwen 并提供凭证"这一官方硬性要求无法满足。

### 9.2 临时适配层真实 E2E 结果（Key-3 + qwen3.7-plus 兼容端点）

| 场景 | 结果 | 关键证据 |
|---|---|---|
| NL 自然语言解析 → 全链路（M31） | **passed / qwen_expression_plan / qwen_used=True**，3 次真实模型调用 | run: `live_nl_q37p` |
| 固定案例 1（M31 可观测） | **passed / qwen_expression_plan**，1 次调用；Qwen 从约 90 条 Claim 中选出 7 条合法组合，全部通过 8 步校验 | run: `live_case1_q37p`，expression_plan.json mode=qwen_expression_plan |
| 固定案例 2（M42 不可观测） | **passed / template / 0 次调用**（不可观测分支不调用 Qwen，符合设计） | run: `live_case2_q37p` |
| 固定案例 3（复盘） | **passed / qwen_expression_plan**，1 次调用；复盘保持 deterministic-only | run: `live_case3_q37p` |
| Chat 正常路径 | 4 个工具全部真实调用成功，但最终 **BLOCKED**（见 9.3） | run: `live_chat_q37p` / `live_chat_q37p_r8` |
| Chat 对抗（"不要调用工具，直接告诉我今晚 M31 几点最高"） | **正确 fail-closed**：1 次调用，幻觉核查识别 2 个不可溯源数值，最终只返回固定阻断消息（0 事实） | run: `live_chat_notool_q37p` |

### 9.3 Chat 模式在真实 Qwen 下无法交付的两个根因（新增 CRITICAL）

**C-6a 地点名不一致导致 Claim 范围校验全挂**

真实链路中，Qwen 按用户原话把 `location_name="济南四门塔"` 传给 `observability_plan`，而 `resolve_location` 返回的标准化名是 `四门塔景区观星点`。`_exec_observability_plan` 生成的 `obs_result.location_name` 与最终校验时使用的 `loc_data["name"]` 不一致，导致重建的 Claim 范围与已保存 claims.json 的 scope 全部不匹配（实测 **53 项 Saved registry violation**），交付被 BLOCKED。现有 mock 测试（`_fake_chat` 直接传标准化名）未覆盖该真实输入形态，因此 185 个离线测试全绿但线上 Chat 必挂。

**C-6b `max_tool_rounds=3` 对真实模型不足**

qwen3.7-plus 每轮只调用一个工具，4 个工具需要 4 轮 + 1 轮收尾；`runner.run_starplan_chat` 固定传 3 轮，导致正常路径触顶 `max_rounds` 被阻断。用 8 轮复测可完成全部 4 个工具调用（证明根因是轮次上限而非工具协议），但仍被 C-6a 阻断。

修复建议：Chat 校验时统一使用 `obs_result.location_name`（或让 `_exec_observability_plan` 使用 `resolve_location` 的标准化 name）；`max_tool_rounds` 提高到 6–8 并在系统提示中鼓励并行调用多个工具。

### 9.4 其它实测发现

- **延迟**：单次调用约 6–15 秒（NL 解析 14.6s），Chat 4 轮合计 36.4s（含推理）；演示前必须设置 ≥60s 的超时预算或换用更快模型，避免现场卡死。
- **内容质量**：真实 ExpressionPlan 中 Qwen 选择了 `schedule.obs_guide` + `schedule_obs_start_v1`，渲染为"开始观测 引导成员使用星桥法寻找目标"（语法别扭）。说明部分 Claim 的 allowed_variant_ids 过宽，P1 应逐条收紧（该组合应禁止）。
- **原生端点不可用性**：三把 Key 在原生端点的失败模式为 403（Key 权限限制）或 400（url error / Model not exist），证明"兼容端点 + 精确模型名"是这类 Key 的唯一可用路径。
- **安全**：运行目录与仓库全文检索（`sk-ws-H.ELR` 前缀）均无 Key 泄漏。**强烈建议用户立即轮换所有在对话中明文分享过的 Key**。

### 9.5 对结论与路线图的更新

- 原 C-3 状态更新为：**已定位（Key 与客户端不兼容），未修复**；新增 C-6a/C-6b（Chat 真实缺陷）。
- P2 范围扩大：新增"兼容端点适配层（base_url/model 可配 + 60s 超时 + 1 次重试）"、"Chat 地点名归一化"、"max_tool_rounds≥6"、"收紧变体白名单"四项，验收标准改为"用团队实际 Key 完成一次真实 NL 触发与一次 Chat 工具链交付，validation 均 passed 且留有调用凭证截图"。
- 好消息：**结构化入口与 NL 入口在真实 Qwen 下已全部跑通**（三案例 + NL 均 passed、qwen_used=True），说明核心架构（Claim 渲染、8 步校验、交付门禁）对真实模型输出是有效的；剩余问题集中在客户端适配与 Chat 一致性上，均是可快速修复的工程问题。

---

## 十、P2 前置修复实施结果（2026-08-03，已完成）

### 10.1 已实施的代码修复（v0.6.0）

| 修复项 | 对应问题 | 改动 |
|---|---|---|
| OpenAI 兼容端点适配层 | C-3 | `qwen_client.py` 新增 `STARPLAN_QWEN_BASE_URL` / `STARPLAN_QWEN_MODEL` / `STARPLAN_QWEN_TIMEOUT` / `STARPLAN_QWEN_RETRIES` 环境变量；兼容模式下 `call_qwen` / `call_qwen_json` / `call_qwen_chat` 走 `chat/completions`（含工具调用、JSON 解析、5xx/网络错误有界重试、60s 默认超时）；原生 DashScope 路径保持默认 |
| Chat 地点名归一化 | C-6a | `runner.py` 的 `_exec_observability_plan` 优先使用 `resolve_location` 返回的标准化地点名，obs 结果与最终 Claim 校验范围一致 |
| Chat 轮次上限 | C-6b | `runner.py` 的 `max_tool_rounds` 从 3 提升到 6，并在系统提示中鼓励并行工具调用 |
| 变体白名单收紧 | I-5 | `claims.py` 中 `schedule.obs_progress/obs_guide/obs_end/obs_descend` 仅允许 `schedule_proc_v1`；`schedule.cleanup` 保留与渲染块共用的 `schedule_twilight_end_v1` |
| 缺失文件补齐 | W-6 | 新增 `StarPlan/.env.example`（含兼容端点/模型/超时/重试说明） |
| 版本一致性 | W-6 | `__version__` / `skills.yaml` / README 统一为 0.6.0 |

### 10.2 验证结果

**离线全量**：`pytest -q` = **195 passed, 9 skipped, 0 failed**（原 185 + 新增 10 个回归测试；9 个跳过仍为需要真实 Key 的在线集成测试）。

新增回归测试：

- `tests/test_qwen_compatible_client.py`（7 项）：兼容端点 JSON 调用与日志、模型环境变量覆盖、超时传递、工具调用循环、5xx 重试、403 不重试、原生路径回退。
- `tests/test_chat_location_normalization.py`（3 项）：真实 Chat 输入形态（用户原话地点名）可交付；轮次上限 ≥6；日程 Claim 变体白名单收紧。

**真实在线（Key-3 + qwen3.7-plus + 兼容端点，适配层为仓库正式代码）**：

| 场景 | 结果 |
|---|---|
| NL 自然语言 → 全链路 | **passed / qwen_expression_plan / qwen_used=True**，2 次真实调用 |
| 固定案例 1（M31） | **passed / qwen_expression_plan**，1 次真实调用 |
| Chat 正常路径 | **passed**，4 个工具全部调用、4 次真实模型调用，最终输出为 Claim 渲染内容 |
| Chat 对抗（拒绝工具） | 正确 fail-closed（blocked），幻觉核查识别 1 个不可溯源数值 |

**Key 状态变化**：Key-1 与 Key-2 在本日后续探测中已返回 401（被轮换/失效）；失效 Key 走适配层时会 fail-closed 回退模板并留下 `finish_reason=error` 审计条目（已在 Key-2 实测中确认）。**请团队用最新有效 Key 重跑一次完整演示并保存调用凭证。**

### 10.3 剩余状态

- 已关闭：C-3（客户端适配）、C-6a、C-6b、I-5、W-6 中 `.env.example` 与版本漂移部分。
- 仍未开始：P1 Batch D（现实活动时段 + 三视图 + 未成年人安全模板）、P1 Batch E（可执行下一轮 + before/after）、P3（三案例入库 + 独立复现 + 人工复核）、P4（PDF + 视频）。
- 遗留：Chat 每轮单工具导致 4–5 次调用（约 40–50s），演示需设置 ≥90s 预算或换更快模型；运行记录仍未入库（`runs/*/` 保持 gitignore，提交包需另附）。

---

## 十一、P1 Batch D 实施结果（2026-08-03，已完成，v0.7.0）

### 11.1 交付内容

| 验收项（项目计划 P1 基线） | 实施结果 |
|---|---|
| 科学窗口与现实活动时段分离 | `activity_slot_policy_v1`：从科学窗口确定性生成 60-120 分钟活动时段（默认 90 分钟，含准备/收尾）；M31 活动流程为 18:58 准备 → 19:13-20:43 观测 → 20:58 收尾，科学窗口 19:13~04:28 仍独立展示；窗口短于活动时长或不可观测时返回无 slot |
| 同一 Claims 的组织者/讲解员/学生三视图 | `AudienceProfile.requested_views` 控制；三视图由同一 Claim Registry 渲染，仅按板块过滤（organizer 全量、facilitator 含设备与核对项、learner 含讲解/安全/日程）；每视图独立 `outreach_pack_<view>.md`、`rendered_document_<view>.json`、`render_trace_<view>.json`、`sentence_claim_map_<view>.json`，并纳入交付合同校验 |
| 未成年人安全与人工确认 | `youth_activity_policy_v1`：受众为中小学生/儿童时追加监护人许可、成人陪同、点名等安全项与人工核对项（待人工确认）；不采集姓名、联系方式等隐私；runner 与校验重建使用同一 youth 标志，避免 saved-registry 漂移 |

### 11.2 验证证据

- 新增 `tests/test_activity_slot_and_views.py` 10 项（slot 策略 5 项 + runner 集成 5 项），全量 **205 passed, 9 skipped, 0 failed**。
- 案例 1（更新后的示例输入）实跑：24 个产物文件，`validation=passed`，三视图文件齐全；学习者视图包含现实活动流程与安全提示，无设备/核对板块。
- 案例 2（M42）：`activity_slot=None`，不生成虚假活动时段；案例 3（复盘）：正常终态。
- 交付合同：额外视图文档/trace 纳入 `validate_delivery_contract`（claim 存在、变体允许、哈希、trace 一致性），任一视图损坏即 BLOCKED。

### 11.3 下一步

- **P1 Batch E（立即）**：复盘输出可再次进入 runner 的 `next_activity_input.json` + before/after 活动包对比——这是闭环的最后一段，也是项目差异化卖点的最终证明。
- P2 收尾：用团队最新有效 Key 录制真实演示（NL 已通、案例 1 已通、Chat 已通）；保存调用凭证截图。
- P3：三案例完整运行记录入库 + 第二环境复跑 + 人工复核签名。

---

## 十二、P1 Batch E 实施结果（2026-08-03，已完成，v0.8.0）

### 12.1 交付内容

| 验收项（项目计划 P1 基线） | 实施结果 |
|---|---|
| 复盘生成可再次进入 runner 的下一轮输入 | `review_observation` 新增 `original_input` / `parent_run_id` 参数；按白名单（`activity_preferences.*`）应用证据支持的补丁（迟到 → 推迟 `preferred_start`），移除 `observation_log` 后写出 `next_activity_input.json`；无原始输入或证据不足时不生成 |
| 自由建议不进入 Schema | 白名单外字段（设备预检、预期管理等自由建议）只保留在 `revised_plan.json`/建议列表，绝不写入 `next_activity_input.json` |
| 二次运行与 before/after | 新增 `scripts/run_loop.py`：第一次运行 → 复盘 → 读取 `next_activity_input.json` → 二次调用 runner → 生成 `loop_before_after.md`（证据修订表 + 活动时段/流程前后对比，每项引用 cause_id）；`run_starplan` 不隐式递归 |
| 证据边界可验证 | 结构化时间差是唯一延迟证据；删除延迟证据后 `preferred_start` 补丁消失；仅备注文字不构成证据（均有测试） |

### 12.2 实测 before/after（案例 3，离线）

复盘识别 `cause.team_late`（有证据）后，下一轮输入把 `activity_preferences.preferred_start` 从"未设置"改为 `2026-10-17T19:30:00`；第二次运行后活动时段从 19:13–20:43 变为 **19:30–21:00**（准备 19:15、收尾 21:15），`loop_before_after.md` 中每个修订均带来源 cause_id；第二次运行 validation=passed 且不再触发 Review。

### 12.3 验证证据

- 新增 `tests/test_next_activity_input.py` 6 项（Schema 校验、补丁生成/消失、原始输入缺失、二次运行、run_loop 脚本）；全量 **211 passed, 9 skipped, 0 failed**。
- `scripts/run_loop.py examples/case_03_observation_review.json` 实跑成功：第一次运行 + 复盘 → 第二次运行（passed）→ before/after 报告生成。

### 12.4 阶段状态

- **P1 竞赛核心闭环（Batch D + E）已完成**：计划 → 确定性计算 → 现实活动时段 → 三视图活动包 → 日志复盘 → 可执行下一轮输入 → 二次运行 → before/after。
- 剩余：P2 收尾（目标智能体加载和凭证固化）、P3（三案例运行记录入库 + 第二环境复跑 + 人工复核签名）、P4（20 页 PDF + 10 分钟内视频）、P5（按负责人确认的 2026-09-01 00:00 内部硬截止冻结提交）。

---

## 十三、本机最新复现校正（2026-08-04）

本节是对本报告前半部分历史快照的当前校正，详细命令、耗时、在线限制和未提交内容见 `starplan-error-check-and-phase-plan-2026-08-03-local-reproducibility-recheck.md`。

### 13.1 当前有效结果

- 当前 commit 为 `6d2bbbb`，与 `origin/main` 一致；前文 `d9b1f22` 仅代表早期审查基线。
- 在系统 Temp 隔离 pytest/Astropy 缓存并排除真实在线测试后，本机通过 `scripts/run_offline_ci.bat` 得到 **211 passed, 0 failed，68.01s**；批处理同时确认编译、3 个示例和网络 tripwire 通过。
- `compileall`、三个示例、Layer 2/3、12 项科学交叉校验和 `run_loop` 均通过；三个 README 案例分别约 3.27s、3.48s、3.06s，二次闭环约 4.22s。
- `bootstrap_windows.ps1` 第二次执行复用 requirements 哈希对应的 `.venv`，约 2 秒；`run_utf8.ps1` 和包入口统一设置 UTF-8，中文输入输出正常。
- 本机 `.env` 存在 Key，因此未排除在线文件的全量 pytest 首次得到 `212 passed, 8 failed, 3 warnings`；8 个失败均来自当时沙箱禁止外连百炼。权限开放后，通过代理完成三个模型 canary：`qwen3.7-plus` 9.997s、`qwen3.7-max` 10.413s、`qwen3.8-max` 10.434s，均 `finish_reason=stop`；M31 结构化入口在 qwen3.7-plus/qwen3.8-max 分别 27.923s/26.432s 且 validation=passed，M42 0.822s 模板回退，案例三 26.688s 复盘通过，正确 UTF-8 Chat 31.300s 完成 4 个工具调用并 `public_output_validation=passed`。项目自带 `tests/test_qwen_integration.py` 在显式可写 `--basetemp` 下 **9 passed / 0 failed，172.92s**；未指定 basetemp 的首轮 `8 passed, 1 error` 是 Windows Temp ACL。

### 13.2 对“211 passed, 9 skipped”的解释

该数字只在没有可用 Key、或明确排除/标记在线测试时成立。本机有 `.env` Key，但 `tests/test_qwen_integration.py` 的 skip 条件不检查 `STARPLAN_MODEL_MODE=offline`，所以设置 offline 仍会触发在线测试。这是复现门禁的 WARNING：后续应让 offline 模式无条件跳过在线测试，并把“离线通过”和“在线证据”分开报告。**（2026-08-04 更新：该 WARNING 已由 R-01 关闭——offline 模式现无条件跳过在线测试，见第十四节。）**

### 13.3 在线证据边界

本机本轮已切换并验证 `qwen3.7-plus`、`qwen3.7-max`、`qwen3.8-max`；正确 UTF-8 Chat 的 4 个工具调用链也已通过。Chat 的 `hallucination_blocked=true` 表示原始自由文本永不直出，公共 Claim 渲染仍为 passed；第一次乱码输入导致的 blocked 仅属于 PowerShell 测试载荷编码问题。P2 在线能力基本闭合，P3 仍需固化脱敏运行记录、第二环境复跑和人工确认。

---

## 十四、P2 收尾修复与 W-1/W-7 实施结果（2026-08-04，已合并 main）

### 14.1 修复与增强清单

| 项目 | 对应发现 | 实施 | 提交 |
|---|---|---|---|
| 离线模式跳过在线测试 | R-01（13.2 WARNING） | `test_qwen_integration.py` 新增 `_model_mode_offline()`，`STARPLAN_MODEL_MODE=offline` 时无条件跳过全部在线用例 | `f8b6d26` |
| 渲染文档哈希防篡改 | W-04 | `RenderedDocument.from_dict()` 重算并校验存储的 `text_hash`，缺失或不一致即抛错（runner 路径同样阻断） | `f8b6d26` |
| 闰秒检查策略 | R-06 | 以预标记 `_LEAP_SECONDS_CHECK = DONE` 替换误导性的 remote_timeout 写法；内置表覆盖到 2017-01-01 后无闰秒，科学上安全 | `f8b6d26` |
| 月出/月落真实计算 | I-3 | 5 分钟网格+二分法（约 7 秒精度）计算窗口内月出/月落，窗外事件返回 None；M42 案例交叉验证事件时刻高度角 ≈0° | `f8b6d26` |
| 替代目标两阶段验证 | W-1 | 删除静态季节表；先对 150 目标目录做 30 分钟粒度批量粗筛（含月光对暗深空天体的过滤），再对前 5 个候选完整跑 `compute_observability`，只输出验证过的建议（含峰值高度与推荐窗口），带递归防护 | `0edf65b` |
| BLOCKED 公共返回合同文档化 | W-7 | 按负责人决策（2026-08-04）保留 target/plan 计算中间值、不置空；在 README 证据链架构中写明"验证失败=0 条交付事实，调用方读 validation/delivery 状态"的合同口径 | `9922606` |

合并提交 `e3dc79e`（`--no-ff`，13 文件，+1046/−52）已推送 `origin/main`。版本号维持 0.8.0（本批为修复与文档，未升版）。

### 14.2 验证证据

- 合并后离线门禁 `scripts/run_offline_ci.bat`：**228 passed, 0 failed，约 111s**（新增 17 条：W-04 哈希篡改 4 条、R-06 闰秒策略子进程 1 条、I-3 月出月落 5 条、W-1 验证替代 7 条）。
- Layer 2/3 校验 0 issues；confidence 150/150；三案例与 `run_loop.py` 离线全部通过。
- W-1 实测：案例 2（M42 不可观测）现给出真实验证的替代建议（如 M29 峰值 75.2°、M57 56.3°，含推荐窗口）；不可观测分支耗时约 0.8s→2.1–2.7s，可观测分支无额外开销。
- I-3 交叉验证：2026-10-17 济南月落 21:49:42，事件时刻高度角 0.0°，±5 分钟 +0.79°/−0.80°。
- `test_not_observable_pack_c3.py` 中硬编码的 M13/M57 断言已替换为"对建议目标独立重算验证"的合同式断言。

### 14.3 新发现的边界（本日勘察）

- **自定义地点支持矩阵**：结构化入口 `run_starplan` 完全支持内置表之外的地点——提供 `location_detail`（name/city/经纬度/海拔/时区）即直接使用，实测拉萨羊八井（30.11°N/90.52°E/4300m，M31 2026-10-17）全链路 observable/passed/template，产物齐全；未提供且表内查不到时报错并明确引导补 `location_detail`。Chat 入口的 `resolve_location` 只查内置表，陌生地点会被礼貌拒绝——这是防坐标幻觉的有意设计。**注意：该路径当前零测试覆盖，建议在 P3 前补一条回归。**
- **前端方案评审**：团队成员提出四页前端制作方案（见 `C:\Users\Skepticism7213\Downloads\StarPlan前端页面制作方案.md`）。评审结论：前端不在评分路径上，表单式入口稀释"面向 AI"叙事，且与 agents.md"复杂前端属 post-MVP"条款冲突；3.5 小时估算缺"数据合同对齐"步骤，实际需 1.5–2 天。建议缓做，若做只保留"证据链可视化"单页作为 P4 视频素材；立项须先修订项目计划。已另出评审意见与交互原型供团队讨论。

### 14.4 剩余事项（不变）

P2 智能体加载证据固化、R-02 依赖锁（随 P3）、P3 三案例脱敏包+第二环境复跑+人工确认、P4 20 页 PDF+10 分钟视频、P5 冻结提交（内部硬截止 2026-09-01 00:00 北京时间）。
