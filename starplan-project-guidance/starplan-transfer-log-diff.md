# Transfer Log 与 Project Plan 的差异清单

日期：2026-07-18  
对照基准：`starplan-loop-project-plan.md`  
已更新文件：`starplan-qoderwork-transfer-log.md`

本文只记录 transfer log 相对当前 project plan 的调整，方便团队审核；它不是第三份独立方案。

## 1. 已修改的核心差异

| 项目 | 原 transfer log | 当前 project plan | 处理结果 |
|---|---|---|---|
| 项目定位 | 观测前计划、星图和科普材料生成 | 观测计划—活动执行—日志复盘—下一次修订的闭环 | 已修改为 `StarPlan Loop` 闭环定位 |
| 一句话简介 | 生成观测计划与科普活动方案 | 让校园观测活动被计划、执行、检查并持续改进 | 已修改 |
| 核心流程 | 到人工校验与复现记录结束 | 增加实际日志、偏差归因和修订计划 | 已修改 |
| 核心 Skill 数量 | 5 个子 Skill：目标、可观测性、太阳系星历、星图、科普包 | 4 个核心 Skill：目标解析、可观测性计划、科普活动包、观测日志复盘 | 已修改 |
| Skill 命名 | `target_resolver`、`observability_planner` 等 | `target_resolve`、`observability_plan`、`outreach_pack`、`observation_review` | 已统一 |
| 图表功能 | `sky_visualizer` 作为独立 Skill | 图表是展示层，不在首期拆成独立 Skill | 已降级为展示层 |
| 太阳系星历 | 首期 Skill 和第二个必跑案例 | 核心稳定后再考虑的扩展 | 已后置 |
| 三类案例 | M31、土星、英仙座流星雨 | 正常可观测、不适合观测及备选、实际日志复盘 | 已改为围绕闭环的案例 |
| 第一阶段最低目标 | M31 生成计算、曲线、活动包和校验报告 | 在此基础上导入日志并生成复盘报告与修订计划 | 已增加闭环验收 |
| 阶段安排 | 第 3 周直接做土星和行星星历 | 第 3 周接 Qwen，第 4 周做日志复盘，第 5 周再做完整案例 | 已调整顺序 |

## 2. 保留但明确降级的内容

以下内容仍在 transfer log 中保留，但不再代表 MVP 必须完成：

- `JPL Horizons`：作为太阳系目标的后续星历来源或按目标类型进行交叉校验。
- `SIMBAD/astroquery`：作为目标信息参考来源；首期先保证目标目录和本地可复现链路。
- `Stellarium/KStars`：作为人工交叉验证和复现工具，不作为核心运行依赖。
- `Aladin Lite/WWT`：作为后续星图或可视化出口，不作为自研平台建设目标。
- 土星观测：核心闭环稳定后可加为扩展示例。
- 流星雨、月食等复杂天象：后续可以使用半结构化事件输入，但不进入首期闭环验收。
- 校园地平线遮挡模型：作为具有场景特色的后期加分项。

## 3. 新增或强化的内容

这些内容是原 transfer log 中不足、但当前 project plan 已明确的部分：

1. **`observation_review`**：读取实际观测日志，区分有证据的原因、可能原因和无法判断的情况。
2. **真实参数修订**：下一次计划必须修改时间、目标优先级、设备准备或流程步骤中的至少一项。
3. **`calculation_manifest.json`**：记录原始需求、结构化参数、目标来源、工具版本、约束规则、中间结果和人工确认。
4. **`model_call_log.jsonl`**：记录 Qwen 模型版本、提示词/工具调用和返回结果，证明阿里云能力的实际使用。
5. **失败场景验收**：增加目标歧义、地点/时区缺失、目标低空、强月光和日志信息不足等测试。
6. **核心计算离线复现**：在线服务不能成为核心演示的单点故障。
7. **轻量展示入口**：页面只负责调用 Skills 和展示证据，不以复杂前端为主要成果。

## 4. 仍然一致的内容

以下内容在两份方案中没有冲突，已保留：

- 参赛方向为赛道三方向三。
- 必须使用 Qwen/阿里云百炼等阿里云 AI 能力。
- Qwen 负责理解、编排和受限表达计划；确定性工具和渲染器负责事实计算与最终事实句。
- 采用 Astropy/astroplan 等工具进行可观测性计算。
- 需要统一输入输出 Schema、Skills 清单、运行记录和验证报告。
- 需要提交源码、技术报告、PPT/PDF、演示视频和可运行/可加载的 Skills 包。
- 2026-07-18 时采用的五人理想分工和六周相对节奏已被 2026-08-02 的三角色、固定截止日期倒排替代，见本文第 7 节。

## 5. 当前建议团队重点审核的三件事

1. 是否同意把“日志复盘和下一次修订”作为项目的主要差异化，而不是可选功能。
2. 首批目标目录是否先限定为固定、可审计的目标集合，避免一开始覆盖所有天体。
3. 是否接受“土星、流星雨、Stellarium/Aladin 等先后置”的范围控制。

只要这三项确认，transfer log 和 project plan 就可以作为同一套执行基准使用。

## 6. 2026-07-26 幻觉防护架构对齐

本次将 `starplan-hallucination-prevention-architecture.md` 中的核心决策合并进 project plan，并同步更新 transfer log。完整字段示例、对抗用例和分阶段实施细节继续以专项架构文档为准。

| 项目 | 旧表述 | 当前 project plan | 对齐结果 |
|---|---|---|---|
| 模型角色 | Qwen 基于事实卡生成讲解稿 | Qwen 只选择 Claims、审核句式、顺序和语气；程序渲染事实句 | 已同步 |
| 幻觉防护 | 自由文本生成后检查数值 | Claim Registry 事前准入，验证失败确定性回退 | 已同步 |
| 文字事实 | 主要约束关键数值 | 数值和文字事实都必须有 Claim 与来源 | 已同步 |
| 失败状态 | 各 Skill 局部处理 | 明确区分待确认、可观测、不可观测、数据不足、工具错误和验证阻断 | 已同步 |
| 证据链 | Manifest、模型日志和校验报告 | 增加 Claims、render trace、RunOutcome、审计事件和产物哈希 | 已同步 |
| 复盘归因 | 引用计划和日志证据 | 使用 Evidence Claims；证据不足时只能标记 `possible` 或 `undetermined` | 已同步 |
| 验收门槛 | 模型只使用事实卡 | 无来源事实率 0、阻断原文泄漏率 0、Claim 映射覆盖率 100% | 已同步 |

## 7. 2026-08-02 赛道复盘与提交倒排对齐

本轮根据赛道原文、当前代码完成度、团队资源和正式提交要求，更新了 project plan 和 transfer log。此次不是扩大功能，而是把执行基线从“理想六周研发计划”改为“截至官方 2026-09-05 截止（内部硬截止 09-01）的竞赛闭环交付”。

| 项目 | 旧基线 | 2026-08-02 当前基线 | 处理结果 |
|---|---|---|---|
| 服务对象 | 以高校天文社和校园观测为主 | 同时覆盖中小学教师、青少年科普组织者、高校社团和非专业组织者 | 已扩展但仍围绕同一观测闭环 |
| 科普输出 | 单一受众活动包 | 同一 Claims 生成组织者、教师或讲解员、学生三类表达 | 纳入核心验收 |
| 演示形态 | 轻量网页或等价入口 | Qwen 智能体直接加载/调用为核心，6 至 8 分钟视频优先，独立前端可选 | 已重新排序 |
| 复盘结果 | 修订建议或 `revised_plan.json` | 必须生成可再次进入 runner 的下一轮输入和重新渲染活动包 | 提升为闭环阻断项 |
| 时间设计 | 推荐窗口直接支撑活动流程 | 分离科学可见窗口、现实活动时段、准备和收尾时间 | 纳入核心代码任务 |
| 未成年人场景 | 一般安全提示 | 固定安全与人工确认清单，模型不能自动确认许可和监护 | 纳入场景边界 |
| 团队资源 | 五人理想分工 | 两名核心投入、合计三名稳定协作 | 已改为三角色安排 |
| 截止与材料 | 六周相对阶段 | 官方 2026-09-05 00:00 截止（内部硬截止 09-01 00:00）；20 页内 PPT/PDF、Skills 包、三类记录、技术报告和源码；视频优先 | 已写入倒排计划 |
| 旧 Phase Plan | Phase A-D 完成声明与后续技术硬化 | 完成声明失效；独立复查缺陷继续跟踪，但按可见竞争影响重排 | 已明确效力 |
| Qwen 启动指令 | 仍要求先输出早期项目启动规划 | 改为基于最新 main、逐批执行 P0-P2、先写失败测试并强制报告/提交的操作 Prompt | 已同步 |

当前 source of truth 是更新后的 `starplan-loop-project-plan.md`。历史完成报告只用于追溯曾做过的修复，不再用于证明当前阶段已验收。

## 8. 2026-08-05 P2 交付方式决策（方案 A）

本轮将 P2 的智能体交付方式从“接入至少一种 Qwen 智能体加载/调用方式”细化为
**方案 A：QoderWork 应用内挂载**，并同步更新 project plan 与 transfer log。

| 项目 | 旧基线 | 2026-08-05 当前基线 | 处理结果 |
|---|---|---|---|
| 交付方式 | 未指定具体智能体环境 | QoderWork 应用内 Skill（`qoderwork-skill/SKILL.md`）+ MCP stdio 适配层 | 已写入 P2 验收 |
| API Key | 在线验证依赖百炼 Key | 不提供、不写入任何 Key；MCP 工具层固定 `STARPLAN_MODEL_MODE=offline` | 已写入 P2 验收 |
| 语言层 | Qwen 通过百炼 API 调用 | QoderWork 应用本体即 Qwen 语言层，负责触发识别、编排与转达 | 已写入 P2 验收 |
| 调用凭证 | 在线 model_call_log 日志 | 应用内录屏（Skill 触发链 + 工具调用链 + 产物核对） | 已写入 P2 验收 |
| 宿主转达 | host_guidance 合同 | QoderWork 只原样引用工具渲染文档，禁止改写数值；BLOCKED 如实告知 | 与 2026-08-05 合同批次一致 |
| 版本 | 0.8.0 | 0.9.0（MCP 适配层 + Skill 清单 + 7 个回归测试） | 已升版 |

截止日期已由负责人核验确认（2026-08-05）：**官方 2026-09-05 00:00（北京时间），
内部硬截止 2026-09-01 00:00（北京时间）不变**；全量文档基线已按此统一，详见第 9 节。

## 9. 2026-08-05 官方截止日期确认

负责人确认赛题官方截止日期为 **2026-09-05 00:00:00（北京时间）**。历史
文档与报告中的 09-01 表述保留为“当时基线/团队内部硬截止”，不再作为官方
日期引用。当前基线：

- 官方截止：2026-09-05 00:00:00（北京时间）。
- 团队内部硬截止：2026-09-01 00:00:00（北京时间），预留 4 天缓冲处理提交包问题。
- 倒排阶段 P5 冻结日为 08-31，09-01 至 09-05 仅允许提交包修复，不再加功能。
